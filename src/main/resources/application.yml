server:
  port: 8080


spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    generate-ddl: true
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: MySQL
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/development
    username: root
    password: Danish@9027

api:
  url: https://jsonmock.hackerrank.com/api/football_matches?
